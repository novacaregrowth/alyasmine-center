import type { Metadata } from "next";
import FreeCoursePage from "./PageClient";
export const metadata: Metadata = {
  title: "Pre-Session Guide",
  description: "Your pre-session welcome guide — four modules shared with every Al Yasmine Center client after booking.",
};
export default FreeCoursePage;
